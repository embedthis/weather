/*
    blink.me -- MakeMe File for the Blink app

    MakeMe files are used by EmbedThis to generate Makefiles
 */

Me.load({
    targets: {
        blink: {
            configurable: true,
            ifdef:        [ 'apps' ],
        },
    },
})
