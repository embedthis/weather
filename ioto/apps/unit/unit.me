/*
    unit.me -- MakeMe File for the unit test app

    MakeMe files are used by EmbedThis to generate Makefiles
 */

Me.load({
    targets: {
        unit: {
            configurable: true,
            ifdef:        [ 'apps' ],
            depends:      [ 'libunit' ],
        },
        libunit: {
            ifdef: [ 'unit' ],
            type: 'lib',
            path: '${BIN}/libapp${ARLIB}',
            headers: [ 'src/*.h' ],
            sources: [ 'src/*.c' ],
        },
    },
})
